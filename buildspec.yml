version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 16
  pre_build:
    commands:
      - echo Instalando dependencias
      - npm install
  build:
    commands:
      - echo Buildando em `date`
      - npm test
  post_build:
    commands:
      - echo Build finalizado em `date`
artifacts:
  files:
    - '**/*'